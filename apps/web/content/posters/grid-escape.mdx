---
title: Grid Escape
slug: grid-escape
publishedOn: 2026-05-20
isFeatured: false
themeColor: "#708090"
frag: |
  precision highp float;

  uniform float uTime;
  uniform vec2 uResolution;

  void main() {
    vec2 uv = gl_FragCoord.xy / uResolution;

    vec2 grid = fract(uv * 20.0);
    float distort = sin(uv.x * 10.0 + uTime) * sin(uv.y * 10.0 + uTime * 1.5);
    grid += distort * 0.1;

    float line = step(0.95, max(grid.x, grid.y));
    vec3 color = vec3(line * 0.7) + vec3(0.1, 0.1, 0.15);

    gl_FragColor = vec4(color, 1.0);
  }
vertex: |
  attribute vec2 uv;
  attribute vec2 position;

  void main() {
    gl_Position = vec4(position, 0, 1);
  }
---

Systems constrain. Structures confine. Every framework promises freedom but delivers another prison. We optimize ourselves into smaller boxes, call it productivity, and wonder why we can't breathe.
